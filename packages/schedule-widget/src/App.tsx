import type { FC } from "react";
import ScheduleWidget from "./ScheduleWidget";

const App: FC = () => {
  return (
    <div>
      Schedule Widget
      <ScheduleWidget />
    </div>
  );
};

export default App;
